FRONTEND: 
*add accessibility + aria stuff
*look at GPT speech mode? have that ball as well. When streaming output, on stream, the ball gets bigger (animataion) on spaces it goes back to original size 
*get network error when sideload manually 


BACKEND:
*choose a suitable backend language 


GENERAL: 
* add tests (test coverage etc...)
*basic auth (need to sign up to services, so can eventually pay for it) 
*create logging, evals, etc... 
*have button to 'fine-tune' will generate ~100examples from previous of your responses and fine tune a model
OR just get a few (5) example responses of USER, and PASS IT AS CONTEXT (for system prompt)? 
*when making the model + prompt, make sure to have explanation for why you chose this method 
*how will deploy (nginx reverse proxy / CORS / change localhost for BOTH frontend and backend at once (dont need to do it manually))
    * setup middleware and stuff for this! 
* create auth for this project especially since dont want everyone to call the LLM if they have the api route. Look at project description as security and privacy is important i


PRIVACY AND security
* did research and microsoft apparently offers openAI BUT FOR ENTERPRISE! (so safer and more secure) 
    *better than implementing myself as microsoft will always update it and have team of researchers who are discovering new attack vectors etc...
* Microsoft Presidio (Highly Recommended):
llm-guard:
*Patrick Mermelstein Lyons_Dissertation-vid.mp4

EVAL:
3. Quantitative Evaluation (The "Scientific" Pillar)
Subjective "it feels like it works" feedback is not enough for a First-Class grade. You need data.

LLM Benchmarking: Compare two or three models (e.g., GPT-4o-mini vs. a local Llama 3). Evaluate them on Latency (response time), Cost, and Accuracy.
CAN EVEN USE LLM AS A JUDGE!