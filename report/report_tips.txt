- Why not RAG
- Idea of In Context Learning 
- state MMLU benchmark of model used (not need to train, as can find it) 
- why not local models?
- why not fine tuning? why foundation models 
- will pretty much be a report on Week 9 NLP lecture slide!
- answer a lot of the 'why not' questions
    - why not finetune? costs and static knowledge + not dynamic enough (think about if have one tone with one user, another tone with another)
-further improvements setion: how general QA can be filled with company info and workers can ask general info about company / project etc... but it is out of scope
-look at nlp week 9 additional materials
    -critique of k-nn method as have strong models, so many trained on 
- talk about structured formatting and tool calling (instead of cot and few shot) etc... (trade offs of each) 
-really bad for local deployment (k-nn method_)
    Implementing k-NN means that for every single incoming email, your system would have to:
Load an embedding model to vectorize the email
.
Query the ChromaDB vector database to calculate similarities
.
Unload the embedding model and load the Llama 3.2 Question Answering model
.
This adds significant overhead. 

talk about literature on how structured formatting and tool calling was made


-talk about synthetic data generation stuff (how moved to know 'persona generation) 
    https://www.youtube.com/watch?v=HIusawrGBN4
-why 'you are a general ai assistant' is not enough

The system prompt needs to be crystal clear that the tone profile only affects style, not the factual content of the reply. And the factual understanding needs reinforcing:

  ☐ allow user to create these custom rules, however in report mention how further additions is allowing IT people to set custom data anonymisation rules (as have access to the backend)


For your Outlook Prototype, I recommend staying Zero-Shot for general_qa to keep it fast and responsive.

BUT, you should use Few-Shot for your generate_sender_profile and generate_thread_note prompts. Why? Because you want those notes to be exactly 2-3 sentences and follow a very specific "style-only" rule. Examples are the best way to ensure the model doesn't start writing sample replies or long biographies.

Do you want a Few-Shot version of your generate_sender_profile prompt to ensure it stays strictly 2-3 sentences?


Moderation API, have general impl but if in a company and working on sensitive stuff (think police cases etc...) then need to implement your own personal content policy? 

go through .TODO and explain everything

talk about defence and security, and OWASP and how number one threat is llm-guard

https://ailuminate.mlcommons.org/benchmarks/general_purpose_ai_chat/1.0-en_us-official-ensemble