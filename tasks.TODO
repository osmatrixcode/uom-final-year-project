DOCUMENTATION:
    ☐ In README...
    ☐ add section on how to load the client
    ☐ add section on how to load the server
        ☐ remember the OPEN AI API
    ☐ make the README really pretty! 


FRONTEND DESIGN:
    ☐ make it look like claude cowork (clean, simple, modern)
        ☐ make it look like claude code (Shift + Tab to alternate)
    ☐ Have conversation history style interface (like copilot, chatgpt, etc...) right now just generates and cannot scroll through past conversations. Have it so can scroll it
        ☐ think however about email draft? (perhaps make it into a shift + tab thing, where shift + tab is the email draft, and then the main chat interface is the general chat interface?)
    ☐ Shift + Tab to alternate between user input and response (like claude code)


BACKEND:
    VECTOR DATABASE:
        ☐ look into chromaDB (like SQLite for vector DB)
    ALL THINGS AI: 
        ☐ use Langchain (much easier and has standard agentic architecture)
        ☐ PROMPT: get a few (5) example responses of USER, and PASS IT AS CONTEXT (for system prompt)? 
        ☐ Have GENERAL chat interface (can ask ANY question) and then use agentic to find out what the response is
            ☐ right now prompt just generates a response email. 
        ☐ use past email threads from the sender as CONTEXT to generate more correct tone of response
        ☐ when receive email from user, generate an MD file for that user (can implement what tone to have for this person, some back history etc...)
            ☐ think claude.init but for each user. Can update? (in chatbox)? 
        ☐ in email draft prompt, {} for email tone (can change?) etc... 
        
    ALL THINGS BACKEND:
        ☐ create logging, traces, etc... 

 
TESTING & EVALUATION:
    ☐ add general tests for the backend and frontend (unit tests, integration tests, etc...)
    ☐ to get high grade, need data
        ☐ benchmark different models (e.g., GPT-4o-mini vs. local Llama 3) on Latency, Cost, and Accuracy
        ☐ use LLM as a judge to evaluate the responses!
    ☐ start looking at evaluation, what makes it better than 


PRIVACY AND SECURITY: 
    ☐ Azure OpenAI has safety + privacy built in (since it's for enterprise)
    ☐ perhaps python libraries for data anonymisation 



