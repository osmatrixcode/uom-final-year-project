BACKEND:
    VECTOR DATABASE:
        ☐ look into chromaDB (like SQLite for vector DB)
    ALL THINGS AI: 
        ☐ PROMPT: get a few (5) example responses of USER, and PASS IT AS CONTEXT (for system prompt) (few shot prompting)? 
        ☐ Have GENERAL chat interface (can ask ANY question) and then use agentic to find out what the response is
            ☐ right now prompt just generates a response email. 
        ☐ use past email threads from the sender as CONTEXT to generate more correct tone of response
        ☐ when receive email from user, generate an MD file for that user (can implement what tone to have for this person, some back history etc...)
            ☐ think claude.init but for each user. Can update? (in chatbox)? 
        ☐ in email draft prompt, {} for email tone (can change?) etc... 
        
    ALL THINGS BACKEND:
        ☐ create logging, traces, etc... 

 
TESTING & EVALUATION:
    ☐ add general tests for the backend and frontend (unit tests, integration tests, etc...)
    ☐ to get high grade, need data
        ☐ benchmark different models (e.g., GPT-4o-mini vs. local Llama 3) on Latency, Cost, and Accuracy
        ☐ use LLM as a judge to evaluate the responses!
    ☐ start looking at evaluation, what makes it better than 


PRIVACY AND SECURITY: 
    ☐ Azure OpenAI has safety + privacy built in (since it's for enterprise)

LAST MINUTE FEATURES:
    ☐ ProfileMD for each email address (can modify tone, give context, etc...) how a
    ☐ View past conversations with a person (and summarise?) 
